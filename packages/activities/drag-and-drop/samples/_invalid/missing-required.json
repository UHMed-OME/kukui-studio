{
  "_comment": "INTENTIONALLY INVALID: every draggable must have an `id`. Empty arrays are allowed now (transient authoring state); this fixture instead omits the required `id` on the chip so the schema still rejects it.",
  "version": "1.0",
  "title": "Label the parts of a plant cell",
  "draggables": [
    { "label": "Nucleus", "correctZones": ["z-nucleus"] }
  ],
  "dropZones": [
    {
      "id": "z-nucleus",
      "rect": { "x": 0.4, "y": 0.4, "w": 0.2, "h": 0.2 }
    }
  ]
}
