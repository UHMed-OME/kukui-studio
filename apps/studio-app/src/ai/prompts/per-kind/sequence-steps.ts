export const PROMPT = `Activity kind: Sequence Steps.

Pedagogical guidance:
- The learner orders a set of steps into the correct sequence (e.g., a procedure, an algorithm, a pathway).
- 5–9 steps is ideal; longer sequences make ordering by elimination too easy and short ones feel trivial.
- Each step should describe one observable action — "establish IV access," not "do IV stuff." Tense should be consistent (imperative is canonical).
- Reuse the same subject across steps (the clinician, the cell, the immune response) so the learner doesn't have to track perspective shifts.
- If two adjacent steps could plausibly be swapped, that's a teachable ambiguity — flag it in the explanation field rather than reordering to remove the ambiguity.`;
