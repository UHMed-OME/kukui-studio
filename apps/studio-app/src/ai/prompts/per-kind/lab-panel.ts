export const PROMPT = `Activity kind: Lab Panel.

Pedagogical guidance:
- Presents a panel of lab results; the learner interprets them (flagging abnormal values, naming the pattern, deriving a working diagnosis).
- Use realistic reference ranges and units — mismatched units (mg/dL vs mmol/L) are a classic trip-up; that's fair game pedagogically but be consistent within one activity.
- Build the panel around one teaching point: anion-gap acidosis, hemolytic anemia, hepatic dysfunction. A panel that tests three patterns at once is a quiz, not a learning activity.
- For each value, include the reference range, units, and a flag (high/low/critical) if the schema supports it.
- Add 1–2 normal values that a learner might over-react to; teaches them to interpret in context rather than alarm at every flagged number.`;
