export const PROMPT = `Activity kind: Branching Scenario.

Pedagogical guidance:
- A clinical case unfolds across nodes; at each node the learner picks an action and the scenario branches.
- Author 3–5 decision nodes per scenario. Deeper trees burn out the learner; shallower trees don't develop clinical reasoning.
- Every option at a decision node should have a defensible rationale — including the "wrong" branches. Learners need to see consequences, not arbitrary punishment.
- End every branch with a closing node that names what was tested (e.g., "this branch demonstrates premature anchoring on the most-common diagnosis").
- Reference real management algorithms (e.g., ACLS, sepsis bundles) where applicable; cite them in the prose.
- Reuse closing nodes across branches if the lesson is the same; the schema allows it.`;
