export const PROMPT = `Activity kind: Matching Pairs.

Pedagogical guidance:
- Two columns of items. The learner clicks an item on the left, then its match on the right (point-to-point click — not drag-and-drop).
- 5–8 pairs is the sweet spot. Fewer feels trivial, more taxes working memory.
- Each pair should test a single, sharp 1:1 relationship — drug ↔ mechanism, organ ↔ function, sign ↔ syndrome. Avoid "pick the closest match" semantics.
- Include 1–2 plausible distractors on the right column if the schema allows unmatched items; they prevent process-of-elimination shortcuts.
- Keep label text short (≤ 6 words) — long labels squeeze the canvas at mobile sizes.`;
