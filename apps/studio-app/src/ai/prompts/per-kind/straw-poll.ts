export const PROMPT = `Activity kind: Straw Poll (live, synchronous).

Pedagogical guidance:
- A single question with 2–8 choices. There is no "correct" answer — straw polls are temperature checks, sentiment readings, or quick prediction prompts, not graded assessments.
- Keep the prompt short (≤ 25 words). Phrase it so a learner can answer in a heartbeat with their gut.
- Choice labels should be short (≤ 8 words). They appear on tap-target buttons and as tally bar labels; long phrasing crowds the chart.
- Spread choices across the full distribution you expect. For confidence checks, use a Likert-ish 4-point spread (Very / Mostly / Shaky / Lost). For prediction polls, include the obvious answers plus one "neither / something else" to capture outliers.
- Avoid loaded or leading wording — the whole point is to capture honest in-the-moment thinking before discussion.
- Use the optional 'description' field on a choice only when the label alone is ambiguous; brevity wins.`;
